{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["batches"],
  "properties": {
    "updated_at": { "type": "string" },
    "seen_ids": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "batches": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["date"],
        "properties": {
          "date": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
          "timezone": { "type": "string" },
          "criteria": { "type": "object" },
          "categories": {
            "type": "object",
            "minProperties": 1,
            "additionalProperties": { "$ref": "#/$defs/videoCategory" }
          },
          "videos": {
            "type": "array",
            "items": { "$ref": "#/$defs/videoItem" },
            "description": "旧版批次结构，仅用于向后兼容"
          }
        },
        "oneOf": [
          { "required": ["categories"] },
          { "required": ["videos"] }
        ]
      }
    }
  },
  "$defs": {
    "videoCategory": {
      "type": "object",
      "required": ["label", "window", "videos"],
      "properties": {
        "label": { "type": "string", "minLength": 1 },
        "window": {
          "type": "object",
          "properties": {
            "all_time": { "type": "boolean" },
            "hours": { "type": "number", "minimum": 1 },
            "days": { "type": "number", "minimum": 1 }
          }
        },
        "top_count": { "type": "number", "minimum": 0 },
        "min_count": { "type": "number", "minimum": 0 },
        "videos": {
          "type": "array",
          "items": { "$ref": "#/$defs/videoItem" }
        }
      },
      "anyOf": [
        { "required": ["top_count"] },
        { "required": ["min_count"] }
      ]
    },
    "videoItem": {
      "type": "object",
      "required": ["id", "title", "summary", "url", "thumbnail", "channel", "views", "max_height"],
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string", "minLength": 1 },
        "summary": { "type": "string", "minLength": 8 },
        "url": {
          "type": "string",
          "pattern": "^https://(www\\.youtube\\.com/watch\\?v=|www\\.bilibili\\.com/video/)"
        },
        "platform": { "type": "string", "enum": ["youtube", "bilibili"] },
        "thumbnail": { "type": "string" },
        "channel": { "type": "string" },
        "subscribers": { "type": "number" },
        "views": { "type": "number" },
        "duration": { "type": "string" },
        "max_height": { "type": "number", "minimum": 720 },
        "score": { "type": "number" },
        "published_at": { "type": "string" }
      }
    }
  }
}
