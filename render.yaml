services:
  - type: web
    name: ai-guide
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PYTHONPATH
        value: .
      - key: HOST
        value: 0.0.0.0
