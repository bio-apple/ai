name: Daily OSS Stars Update

on:
  # 定时由 daily-refresh.yml（北京 00:00 串行）统一触发；本文件仅保留手动
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  actions: write

concurrency:
  group: daily-oss
  cancel-in-progress: false

jobs:
  update-oss:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"
          cache: npm

      - name: Install npm deps
        run: ASTRO_TELEMETRY_DISABLED=1 npm ci

      - name: Recollect OSS by AI app (≥50k Top3 + 中文Top1 / app)
        id: fetch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/fetch_oss_stars.py

      - name: Commit and push
        id: commit
        if: steps.fetch.outcome == 'success'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          npx prettier --write data/oss-projects.json oss-projects.json
          git add data/oss-projects.json oss-projects.json
          if git diff --staged --quiet; then
            echo "No OSS updates today"
            echo "pushed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          DATE=$(TZ=Asia/Shanghai date +%Y-%m-%d)
          git commit -m "chore: daily OSS recollect $DATE (≥50k Top3 + 中文Top1)"
          git push
          echo "pushed=true" >> "$GITHUB_OUTPUT"

      - name: Trigger Pages deploy
        if: steps.commit.outputs.pushed == 'true'
        uses: actions/github-script@v8
        with:
          script: |
            const sleep = (ms) => new Promise((r) => setTimeout(r, ms));
            let lastErr;
            for (let i = 1; i <= 4; i++) {
              try {
                await github.rest.actions.createWorkflowDispatch({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  workflow_id: 'deploy.yml',
                  ref: 'main',
                });
                console.log(`Dispatched deploy.yml (attempt ${i})`);
                return;
              } catch (err) {
                lastErr = err;
                console.warn(`Dispatch attempt ${i} failed: ${err.message}`);
                if (i < 4) await sleep(1000 * Math.pow(2, i));
              }
            }
            core.setFailed(`Trigger Pages deploy failed after retries: ${lastErr && lastErr.message}`);

      - name: Open issue on fetch failure
        if: failure()
        uses: actions/github-script@v8
        with:
          script: |
            const title = '[ops] Daily OSS stars fetch failed';
            const body = [
              '每日开源精选重刷失败（按 AI 应用分类 · ≥5万 Top3 + 中文Top1）。',
              '',
              `- Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}`,
              '',
              '处置：Actions → Daily OSS Stars Update → Run workflow。',
              '详见 docs/OPS-RUNBOOK.md',
              '',
              '<!-- daily-oss-fetch -->',
            ].join('\n');
            try {
              const { data: issues } = await github.rest.issues.listForRepo({
                owner: context.repo.owner,
                repo: context.repo.repo,
                state: 'open',
                per_page: 30,
              });
              const existing = issues.find(i => i.title === title);
              if (existing) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: existing.number,
                  body,
                });
              } else {
                await github.rest.issues.create({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  title,
                  body,
                });
              }
            } catch (err) {
              core.warning(`无法创建/更新 Issue（仓库可能关闭了 Issues）: ${err.message}`);
              core.summary.addHeading(title).addRaw(body).write();
            }
